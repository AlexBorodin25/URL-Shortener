# URL-Shortener
This is a simple URL Shortener
To use, first install these packages by using "pip install fastapi uvicorn python-multipart"
Then start the app with "uvicorn main:app --reload"
A webpage will be opened where you can shorten your URL
